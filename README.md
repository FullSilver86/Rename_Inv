# Rename_Inv
Renaming invoices in .pdf by searching for company name in file (based on given list). It also searches for number of invoice starting with 000 and names 
that file with invoice number ( number of characters is set and can be changed). 
Used by accountants in my company
